import { Nav } from '../../../components/nav';
export default function EventDetails({params}:{params:{eventId:string}}){return <main><Nav/><section className="mx-auto max-w-4xl p-6"><div className="glass rounded-3xl p-8"><p className="text-aqua">Event #{params.eventId}</p><h1 className="mt-2 text-4xl font-black">AttendX verified session</h1><p className="mt-4 text-white/70">Join with wallet + World ID, approve token allowance, stake, attend, then claim rewards when the event ends.</p><button className="mt-8 rounded-xl bg-neon px-6 py-3 font-bold">Verify & Stake</button></div></section></main>}
