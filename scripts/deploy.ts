import { ethers } from 'hardhat';
async function main(){ const [deployer]=await ethers.getSigners(); const router=process.env.WORLD_ID_ROUTER || '0x0000000000000000000000000000000000000000'; const Token=await ethers.getContractFactory('AttendXToken'); const token=await Token.deploy(deployer.address); await token.waitForDeployment(); const World=await ethers.getContractFactory('AttendXWorldID'); const world=await World.deploy(router,1,process.env.WORLD_ID_APP_ID||'',process.env.WORLD_ID_ACTION||'attendx-verify',deployer.address); await world.waitForDeployment(); const Core=await ethers.getContractFactory('AttendXCore'); const core=await Core.deploy(await token.getAddress(), await world.getAddress(), deployer.address); await core.waitForDeployment(); console.log({token:await token.getAddress(),worldId:await world.getAddress(),core:await core.getAddress()}); }
main().catch(e=>{console.error(e);process.exit(1)});
